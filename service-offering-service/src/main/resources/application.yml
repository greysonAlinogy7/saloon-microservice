spring:
  application:
    name: service-offering-service

  datasource:
    url: jdbc:postgresql://localhost:5432/service-offering
    username: postgres
    password: Alinogy7

  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 5004

eureka:
  instance:
    hostname: localhost
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: "http://localhost:8070/eureka/"